{
  "name": "asia",
  "version": "1.0.0",
  "description": "asia",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "author": "brtq",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
